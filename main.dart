import 'package:flutter/material.dart';
import 'widget.dart';

void main() {
  runApp(MaterialApp(
      home: Scaffold(
          body: ListView(
    children: [
      logoSection,
      loginSection,
    ],
  ))));
}
